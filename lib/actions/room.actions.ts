import { liveblocks } from "../liveblocks"
import {nanoid} from 'nanoid'
export const createDocument = ({email, userId}:CreateDocumentParams)=>{
    // const room = liveblocks.createRoom(
    //     nanoid(),
    // )
}